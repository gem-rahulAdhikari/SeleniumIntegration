steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - compute
  - ssh
  - --zone=asia-south2-c
  - seleniumtest-1
  - --project=g-codeeditor
  - --command='bash -s'
  - --command='/home/gemcodeeditor/script.sh'
options:
  logging: CLOUD_LOGGING_ONLY
