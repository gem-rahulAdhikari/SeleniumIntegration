steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - compute
  - ssh
  - --zone=asia-south1-c
  - seleniumtest-1
  - --project=gem-code--editor
  - --command='bash -s'
  - home/honey_gemini01/my_script.sh
options:
  logging: CLOUD_LOGGING_ONLY
