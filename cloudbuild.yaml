steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - compute
  - ssh
  - --zone=asia-south2-c
  - g-editor
  - --project=g-codeeditor
  - --command='bash -s'
  - --args:
  - -- - '/home/gemcodeeditor/script.sh'
  - -- - 'param1'
options:
  logging: CLOUD_LOGGING_ONLY
